#include "main.h"
/**
 * mul - Multiply two numbers
 * @a: Defines the first intigers
 * @b: Deefines the second intigers
 * Return: Always 0
 */
int mul(int a, int b)
{
	int a = 40;
	int b = 3;

	mul(a, b);
	_putchar(mul);
	return (0);
}
